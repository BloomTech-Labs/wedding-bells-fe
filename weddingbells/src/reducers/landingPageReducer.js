const initialState = {
    authModalVisible: false,
}

export const landingPageReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}